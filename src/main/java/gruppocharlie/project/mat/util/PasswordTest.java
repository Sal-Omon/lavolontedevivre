/*package gruppocharlie.project.mat.util;

import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;

public class PasswordTest {
    public static void main(String[] args) {
        BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();
        String rawPassword = "test";  // La tua password in chiaro
        String encodedPassword = encoder.encode(rawPassword);  // La password criptata
        System.out.println("Password criptata: " + encodedPassword);
    }
}*/
